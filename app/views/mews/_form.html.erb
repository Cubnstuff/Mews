<%= form_for(@mew) do |f| %>
  <% if @mew.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@mew.errors.count, "error") %> prohibited this mew from being saved:</h2>

      <ul>
      <% @mew.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :message %><br>
    <%= f.text_field :message %>
  </div>
  <div class="field">
    <%= f.hidden_field :user_id, value: current_user.id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
